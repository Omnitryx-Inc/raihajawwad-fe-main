# Couple photo(s)

Drop a photo here (e.g. `hero.jpg`) and set `siteConfig.media.heroPhoto` in `src/lib/site.config.ts` to its path
to have it appear behind the hero. Leave `heroPhoto` as `null` (the default) and the hero renders its ink/
burgundy/gold gradient treatment instead, no photo required.
